; clawd examples — 03 LDR (light sensor)
; Reads the on-board LDR of a CYD / ESP32-2432S028R from GPIO34. No external wiring.
; GPIO34 is an input-only ADC pin.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
