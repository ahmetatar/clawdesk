; clawd examples — 02 Blink RGB
; Drives the on-board RGB LED of a CYD / ESP32-2432S028R. No external wiring.
; The LED is active-low: LOW lights it, HIGH turns it off.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
